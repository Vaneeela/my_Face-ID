# my_Face-ID
A face recognition application that uses MTCNN for face detection and Inception ResNet V1 (FaceNet) for face recognition.

The app is trained to recognize two individuals (including Donald Trump) by extracting facial embeddings and matching them against stored references.
